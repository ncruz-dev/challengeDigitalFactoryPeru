spring:
  application:
    name: digitalFactoryPeru-test
  r2dbc:
    # Mantener la base en memoria entre conexiones durante los tests
    url: r2dbc:h2:mem:///test-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ''
    pool:
      initial-size: 5
      max-size: 10
  sql:
    init:
      mode: always

